name: Testing code

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check code
        uses: actions/setup-node@v2
        with:
          node-version: 14
      - run: npm i
      - run: npm run lint
